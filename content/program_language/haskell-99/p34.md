---
title: "P34 - 欧拉函数"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "数论"]
---

# P34 - 欧拉函数

> Calculate Euler's totient function $\varphi(m)$
>
> 官方模块：`Problems.P34`　核心函数：`totient`

---

[← P33 判断互质](./p33) | [P35 质因数分解 →](./p35)

---

## 题目描述

欧拉函数 $\varphi(m)$ 表示 $1\le r\le m$ 中与 $m$ 互质的整数个数。

## 函数签名

```haskell
totient :: Integral a => a -> a
```

## 实现

### 方法一：筛选计数（定义直译）

```haskell
totient :: Integral a => a -> a
totient m
  | m < 1     = error "totient: positive input required"
  | otherwise = fromIntegral . length $ filter (coprime m) [1..m]
```

定义的直接翻译：从 1 到 m 筛出与 m 互质的数，再计数。P37 会用欧拉乘积公式将它优化。

### 方法二：递归遍历

```haskell
totient :: Integral a => a -> a
totient 1 = 1
totient n = go 1 0
  where
    go m acc
      | m > n     = acc
      | coprime m n = go (m + 1) (acc + 1)
      | otherwise   = go (m + 1) acc
```

不用 `filter` 和 `length`，手动递归计数。减少了列表构造的内存开销。

### 方法三：内联 gcd 检查（不依赖 P33）

```haskell
totient :: Integral a => a -> a
totient 1 = 1
totient n = fromIntegral $ length [x | x <- [1..n], myGCD x n == 1]
```

不依赖 P33 的 `coprime`，直接调用 P32 的 `myGCD`。本质上和方法一相同，但依赖关系更浅。

## 方法对比

| 方法 | 代码量 | 依赖 | 特点 |
|------|--------|------|------|
| filter + coprime | 1 行 | P33 | 最抽象，语义清晰 |
| 递归遍历 | 6 行 | P33 | 无中间列表 |
| 列表推导 + myGCD | 1 行 | P32 | 最少外部依赖 |

## 测试

```haskell
>>> totient 1
1
>>> totient 10
4
>>> totient 315
144
```

因为 1、3、7、9 与 10 互质，所以 $\varphi(10)=4$。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P34.html)
- [P37 欧拉乘积公式](./p37)
- [Haskell 99 题学习路线](./)
