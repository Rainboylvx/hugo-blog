---
title: "P01 - 取列表最后一个元素"
date: 2026-07-11
draft: false
toc: true
tags: ["Haskell", "99题", "列表"]
---

# P01 - 取列表最后一个元素

> Last element of a list
>
> 官方模块: `Problems.P01`
> 核心函数: `myLast`

---

[P02 取列表倒数第二个元素 →](./p02)

---


---

[P02 取列表倒数第二个元素 →](./p02)

---


## 题目描述

找出列表的最后一个元素。如果列表为空，返回 `Nothing`。

这是列表类问题中最基础的一道，目的是熟悉 Haskell 的递归模式匹配和 `Maybe` 类型。

## 函数签名

```haskell
myLast :: [a] -> Maybe a
```

输入一个列表，返回 `Just` 最后一个元素，空列表返回 `Nothing`。

## 思路

Haskell 处理列表的经典手法是**递归 + 模式匹配**。列表有两种 case：

- 空列表 `[]`——直接返回 `Nothing`
- 列表有元素——需要区分最后一个元素和中间元素：
  - 只有一个元素 `[x]`（即 `x:[]`），那 `x` 就是最后一个，返回 `Just x`
  - 至少有两个元素 `x:xs`，那就递归地找 `xs` 的最后一个元素

## 实现

### 方法一：标准递归

这是最直接、最符合 Haskell 风格的写法：

```haskell
myLast :: [a] -> Maybe a
myLast []      = Nothing
myLast [x]     = Just x
myLast (_:xs)  = myLast xs
```

模式匹配天然处理了三种情况：

- `[]`——空列表
- `[x]`——单元素列表，等价于 `(x:[])`
- `(_:xs)`——至少两个元素，用 `_` 忽略头部，递归处理尾部

这种写法的好处是**可读性极强**，几乎就是题目描述的直接翻译。

### 方法二：使用 `last` + `Maybe` 包装

Haskell 标准库已经有 `Prelude.last` 函数，但它对空列表抛异常。我们可以封装一层：

```haskell
import Prelude hiding (last)

myLast :: [a] -> Maybe a
myLast [] = Nothing
myLast xs = Just (Prelude.last xs)
```

或者更紧凑地用 `maybe` 和 `listToMaybe`（来自 `Data.Maybe`）：

```haskell
import Data.Maybe (listToMaybe)

myLast :: [a] -> Maybe a
myLast = listToMaybe . reverse
```

`reverse` 把列表反转，`listToMaybe` 取反转后列表的第一个元素（即原列表的最后一个），对空列表自动返回 `Nothing`。

### 方法三：使用 `foldl'`

函数式风格还可以用 fold 实现：

```haskell
myLast :: [a] -> Maybe a
myLast [] = Nothing
myLast xs = Just . foldl' (\_ x -> x) (head xs) $ xs
```

但这种方法不如模式匹配直观，仅供理解 fold 的参考。

## 测试

用官方给出的例子验证：

```haskell
-- 基础测试
>>> myLast [1,2,3,4]
Just 4

>>> myLast ['x','y','z']
Just 'z'

>>> myLast []
Nothing

-- 单元素列表
>>> myLast [42]
Just 42

-- 嵌套列表（注意类型是 List of Lists）
>>> myLast [[1,2],[3,4]]
Just [3,4]
```

## 要点总结

- **模式匹配**是处理列表递归的基础工具，`[]`、`[x]`、`(x:xs)` 三种模式覆盖所有情况
- 用 `Maybe` 类型处理「可能失败」的操作，是 Haskell 的最佳实践，比抛出异常更安全
- 这道题虽然简单，但它建立的递归模式（处理空/一个/多个三种情况）会贯穿 P01-P99

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P01.html)
- [Haskell 99 题学习路线](./)

---

[P02 取列表倒数第二个元素 →](./p02)
