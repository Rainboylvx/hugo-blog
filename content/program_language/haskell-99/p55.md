---
title: "P55 - 构造完全平衡二叉树"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "二叉树"]
---

# P55 - 构造完全平衡二叉树

> Construct completely balanced binary trees
>
> 官方模块：`Problems.P55`　核心函数：`completelyBalancedTrees`

---

[← P54 二叉树定义](./p54) | [P56 对称二叉树 →](./p56)

---

## 题目描述

完全平衡二叉树要求每个节点的左右子树节点数之差不超过 1。给定总节点数，生成所有可能形状。

## 函数签名

```haskell
completelyBalancedTrees :: Int -> [Tree ()]
```

## 实现

### 方法一：列表推导 + 递归

```haskell
completelyBalancedTrees :: Int -> [Tree ()]
completelyBalancedTrees 0 = [Empty]
completelyBalancedTrees n
  | n < 0     = []
  | leftSize == rightSize = combine leftSize rightSize
  | otherwise = combine leftSize rightSize ++ combine rightSize leftSize
  where
    leftSize  = (n - 1) `div` 2
    rightSize = n - 1 - leftSize
    combine a b =
      [Branch () l r | l <- completelyBalancedTrees a
                     , r <- completelyBalancedTrees b]
```

根占一个节点，剩余 $n-1$ 个节点尽量均分。$n-1$ 为偶数时两边相同；奇数时左右可以交换。

### 方法二：do 记法

```haskell
completelyBalancedTrees :: Int -> [Tree ()]
completelyBalancedTrees 0 = [Empty]
completelyBalancedTrees n
  | n < 0     = []
  | otherwise = do
      left <- completelyBalancedTrees a
      right <- completelyBalancedTrees b
      [Branch () l r | l <- leftOptions, r <- rightOptions]
  where
    a = (n - 1) `div` 2
    b = n - 1 - a
    leftOptions = [a, b]
    rightOptions = [b, a]
```

用 `do` 记法表达组合逻辑，对初学者更友好。

### 方法三：加 Memoization 的版本

如果重复调用相同 n，可以缓存结果：

```haskell
import Data.MemoCombinators (integral, memo)

completelyBalancedTrees :: Int -> [Tree ()]
completelyBalancedTrees = memo integral balanced
  where
    balanced 0 = [Empty]
    balanced n
      | n < 0     = []
      | a == b    = combine a b
      | otherwise = combine a b ++ combine b a
      where
        a = (n - 1) `div` 2
        b = n - 1 - a
    combine a b = [Branch () l r | l <- balanced a, r <- balanced b]
```

避免重复计算，适合大量调用的场景。

## 测试

```haskell
>>> length (completelyBalancedTrees 4)
4
>>> completelyBalancedTrees 0
[Empty]
>>> all ((== 4) . treeSize) (completelyBalancedTrees 4)
True
```

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P55.html)
- [Haskell 99 题学习路线](./)
