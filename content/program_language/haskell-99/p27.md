---
title: "P27 - 互斥分组"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "组合"]
---

# P27 - 互斥分组

> Group elements into disjoint subsets
>
> 官方模块：`Problems.P27`　核心函数：`disjointGroups`

---

[← P26 生成所有 K 元组合](./p26) | [P28 按子列表长度排序 →](./p28)

---

## 题目描述

给定各组的指定大小（列表长度不限，元素互不相同），将元素划分成互斥的若干子组，返回所有可能的分组方案。

例如：`disjointGroups [2,3,4] [1..9]` 表示将 9 个元素分成大小为 2、3、4 的三个组。

## 函数签名

```haskell
disjointGroups :: [Int] -> [a] -> [[[a]]]
```

返回 `[[[a]]]`——外层列表是各种分组方案，每个方案是一个组列表，每个组是一个元素列表。

## 实现

### 方法一：递归 choose（标准回溯）

```haskell
import Data.List (foldl')

disjointGroups :: [Int] -> [a] -> [[[a]]]
disjointGroups [] xs
  | null xs   = [[]]
  | otherwise = []
disjointGroups (n:ns) xs =
  [ group : groups
  | (group, rest) <- choose n xs
  , groups <- disjointGroups ns rest
  ]

choose :: Int -> [a] -> [([a], [a])]
choose 0 xs     = [([], xs)]
choose _ []     = []
choose n (x:xs)
  | n < 0 = []
  | otherwise =
      [ (x : picked, rest) | (picked, rest) <- choose (n - 1) xs ]
      ++
      [ (picked, x : rest) | (picked, rest) <- choose n xs ]
```

核心递归：先为第一个组大小 N 生成所有可能组合以及剩余元素，再递归处理剩下的组。

`choose n xs` 返回所有 `(选中的 N 个元素, 剩余的元素)` 对。

### 方法二：基于 P26 的 combinations

```haskell
import Data.List (delete, foldl')

disjointGroups :: [Int] -> [a] -> [[[a]]]
disjointGroups [] _ = [[]]
disjointGroups (n:ns) xs =
  [ group : groups
  | group <- combinations n xs
  , let remaining = foldl' (flip delete) xs group
  , groups <- disjointGroups ns remaining
  ]
```

用 P26 的 `combinations` 生成第一个组的组合，`delete` 逐个删除已选元素得到剩余列表，再递归。

> 注意：`delete` 只在元素类型有 `Eq` 约束时有效，`combinations` 不需要 `Eq`，所以此方法有额外的类型限制。

### 方法三：分组大小相同的情况简化

如果各组大小相等（如 `[2,2,2]`），可以用 `splits` 来简化：

```haskell
disjointGroups :: Int -> [a] -> [[[a]]]
disjointGroups n xs
  | length xs `mod` n == 0 = go (length xs `div` n) xs
  | otherwise              = []
  where
    go 0 xs  = [[]]
    go k xs =
      [ take n xs : groups
      | groups <- go (k - 1) (drop n xs)
      ]
```

但这只适用于不考虑元素顺序的简单划分，不是标准解。

## 方法对比

| 方法 | 特点 | 类型约束 |
|------|------|----------|
| 递归 choose | 标准回溯，无额外类型约束 | 无 |
| combinations + delete | 语义清晰，复用已有函数 | `Eq a` |
| 简化版 | 只用等大小分组 | 无 |

## 测试

```haskell
>>> length (disjointGroups [2,3,4] [1..9])
1260
>>> disjointGroups [1,1] "ab"
[["a","b"],["b","a"]]
>>> disjointGroups [2,2] [1,2,3]
[]
```

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P27.html)
- [Haskell 99 题学习路线](./)
