---
title: "P96 - 检查 Ada 标识符语法"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "解析"]
---

# P96 - 检查 Ada 标识符语法

> Syntax checking for Ada identifiers
>
> 官方模块：`Problems.P96`　核心函数：`isIdentifier`

---

[← P95 英文数字单词](./p95) | [P97 数独 →](./p97)

---

## 语法规则

标识符必须以英文字母开头，之后可以包含字母、数字和下划线；下划线不能连续，也不能位于末尾。

## 实现

### 方法一：递归解析

```haskell
isIdentifier :: String -> Bool
isIdentifier [] = False
isIdentifier (first:rest) = asciiLetter first && consume False rest
  where
    consume previousUnderscore [] = not previousUnderscore
    consume previousUnderscore (c:cs)
      | asciiLetter c || asciiDigit c = consume False cs
      | c == '_' && not previousUnderscore = consume True cs
      | otherwise = False

asciiLetter c = c `elem` ['A'..'Z'] || c `elem` ['a'..'z']
asciiDigit c  = c `elem` ['0'..'9']
```

状态 `previousUnderscore` 记录前一个字符是否为下划线；输入结束时它还为真，就说明标识符以下划线结尾。

### 方法二：按下划线分组

```haskell
isIdentifier :: String -> Bool
isIdentifier input = case splitUnderscores input of
  [] -> False
  firstGroup : groups ->
    not (null firstGroup)
    && asciiLetter (head firstGroup)
    && all validGroup (firstGroup : groups)
  where
    validGroup group =
      not (null group) && all (\c -> asciiLetter c || asciiDigit c) group

splitUnderscores :: String -> [String]
splitUnderscores = foldr step [""]
  where
    step '_' groups = "" : groups
    step c (group : groups) = (c : group) : groups
    step _ [] = []
```

连续下划线、开头下划线或结尾下划线都会产生空分组，因此只需检查每组非空，再检查组内字符。首组还要额外保证第一个字符是字母。这个实现不依赖正则表达式包。

## 方法对比

| 方法 | 状态 |
|------|------|
| 递归解析 | 记录前一个字符是否为下划线 |
| 分组验证 | 用空分组表示非法下划线位置 |

## 测试

```haskell
>>> isIdentifier "this_is_a_long_identifier"
True
>>> isIdentifier "This_ends_in_an_underscore_"
False
>>> isIdentifier "This__has__two__underscores"
False
>>> isIdentifier "1234"
False
>>> isIdentifier "Fibonacci_sequence_1_1_2_3_5_8"
True
```

算法单次扫描字符串，时间复杂度 $O(n)$。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P96.html)
- [Haskell 99 题学习路线](./)

---

[← P95 英文数字单词](./p95) | [P97 数独 →](./p97)
