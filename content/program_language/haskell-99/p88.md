---
title: "P88 - 图的连通分量"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "图"]
---

# P88 - 图的连通分量

> Connected components
>
> 官方模块：`Problems.P88`　核心函数：`connectedComponents`

---

[← P87 深度优先遍历](./p87) | [P89 二分图判定 →](./p89)

---

## 思路与实现

### 方法一：标准解法

从尚未归类的最小顶点出发做 P87 的 DFS，所得可达集合就是一个连通分量；从剩余顶点中删除它并重复。

```haskell
connectedComponents :: G -> [[Vertex]]
connectedComponents graph = collect allVertices
  where
    allVertices = fst (canonical graph)
    collect remaining
      | Set.null remaining = []
      | otherwise = component : collect
          (Set.difference remaining (Set.fromList component))
      where
        component = depthFirst graph (Set.findMin remaining)
```

### 方法二：逐边合并分量

```haskell
connectedComponentsByEdges :: G -> [[Vertex]]
connectedComponentsByEdges graph =
  map Set.toAscList (foldl mergeEdge initial (Set.toList edges))
  where
    (vertices, edges) = canonical graph
    initial = map Set.singleton (Set.toList vertices)

    mergeEdge components (u, v)
      | componentU == componentV = components
      | otherwise = Set.union componentU componentV
          : filter (\component -> component /= componentU && component /= componentV)
                   components
      where
        componentU = componentOf u components
        componentV = componentOf v components

    componentOf vertex = head . filter (Set.member vertex)
```

初始时每个顶点各自形成分量。每读取一条边，就合并端点所在集合；所有边处理完后，剩余集合正是连通分量。这与按顶点执行 DFS 的方法形成对照。

## 方法对比

| 方法 | 输入驱动 |
|------|----------|
| 重复 DFS | 从尚未访问的顶点扩展 |
| 逐边合并 | 由边连接顶点集合 |

## 测试

```haskell
>>> let graph = toG (Paths [[1,2,3,4,5],[2,4],[6,7]])
>>> sort (map sort (connectedComponents graph))
[[1,2,3,4,5],[6,7]]
>>> connectedComponents (toG (Lists ([],[])))
[]
>>> sort (map sort (connectedComponentsByEdges graph))
[[1,2,3,4,5],[6,7]]
```

各次 DFS 访问的顶点集合互不相交，整体复杂度仍为 $O(|V|+|E|)$。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P88.html)
- [Haskell 99 题学习路线](./)

---

[← P87 深度优先遍历](./p87) | [P89 二分图判定 →](./p89)
