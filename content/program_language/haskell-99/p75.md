---
title: "P75 - Maybe Monad 校验输入"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "Monad"]
---

# P75 - Maybe Monad 校验输入

> Maybe monad
>
> 官方模块：`Problems.P75`　核心函数：`maybeGoldbach`

---

[← P74 不用 do 的 IO](./p74) | [P76 Either Monad →](./p76)

---

## 题目描述

解析字符串并验证它是大于 2 的偶数，然后返回原数及其哥德巴赫分解。任一步失败都返回 `Nothing`。

## 实现

### 方法一：do 记法

```haskell
import Control.Monad (guard)
import Text.Read (readMaybe)

maybeGoldbach :: String -> Maybe (Integer, (Integer, Integer))
maybeGoldbach text = do
  n <- readMaybe text
  guard (n > 2)
  guard (even n)
  pure (n, goldbach n)
```

`readMaybe` 处理解析失败，`guard False` 在 `Maybe` 中产生 `Nothing`。`do` 把三种失败路径串成一条直线。

### 方法二：模式匹配版（不需 Maybe Monad）

maybeGoldbach :: String -> Maybe (Integer, (Integer, Integer))
maybeGoldbach text =
  case readMaybe text of
    Nothing -> Nothing
    Just n
      | n <= 2 || odd n -> Nothing
      | otherwise -> let (a, b) = goldbach n
                    in Just (n, (a, b))

用 case 显式处理，不依赖 Monad 抽象。

## 测试

```haskell
>>> maybeGoldbach "28"
Just (28,(5,23))
>>> maybeGoldbach "27"
Nothing
>>> maybeGoldbach "hello"
Nothing
```

`Maybe` 能表达成功或失败，却不能说明失败原因；P76 用 `Either` 补上错误信息。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P75.html)
- [Haskell 99 题学习路线](./)

---

[← P74 不用 do 的 IO](./p74) | [P76 Either Monad →](./p76)
