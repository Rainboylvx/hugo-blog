---
title: "P77 - List Monad 枚举随机游走路径"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "Monad"]
---

# P77 - List Monad 枚举随机游走路径

> List monad
>
> 官方模块：`Problems.P77`　核心函数：`randomWalkPaths`

---

[← P76 Either Monad](./p76) | [P78 Writer Monad 与 Collatz →](./p78)

---

## 题目描述

从位置 0 出发，每一步可以移动 `-1`、`0` 或 `1`。返回长度为 $n$ 的所有选择所形成的位置路径，路径包含起点。

## 实现

### 方法一：list monad do 记法

```haskell
randomWalkPaths :: Int -> [[Int]]
randomWalkPaths n
  | n < 0     = []
  | otherwise = map reverse (walk n)
  where
    walk 0 = [[0]]
    walk k = do
      reversedPath@(position:_) <- walk (k - 1)
      step <- [-1, 0, 1]
      pure ((position + step) : reversedPath)
```

列表 Monad 把每次 `step` 的三个可能分支与已有路径做笛卡尔积。内部反向保存路径可用 `(:)` 常数时间扩展，最后统一 `reverse`。

### 方法二：列表推导式

randomWalkPaths :: Int -> [[Int]]
randomWalkPaths n
  | n < 0     = []
  | otherwise = map reverse (walk n)
  where
    walk 0 = [[0]]
    walk k = [x : xs | x <- [(-1), 0, 1], xs <- walk (k - 1)]

列表推导是 List Monad 的语法糖。

## 测试

```haskell
>>> randomWalkPaths 1
[[0,-1],[0,0],[0,1]]
>>> length (randomWalkPaths 4)
81
>>> all ((== 5) . length) (randomWalkPaths 4)
True
```

$n$ 步共有 $3^n$ 条路径，输出规模本身就是指数级。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P77.html)
- [Haskell 99 题学习路线](./)

---

[← P76 Either Monad](./p76) | [P78 Writer Monad 与 Collatz →](./p78)
