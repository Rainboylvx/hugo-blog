---
title: "P76 - Either Monad 返回错误原因"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "Monad"]
---

# P76 - Either Monad 返回错误原因

> Either monad
>
> 官方模块：`Problems.P76`　核心函数：`eitherGoldbach`

---

[← P75 Maybe Monad](./p75) | [P77 List Monad →](./p77)

---

## 题目描述

重写 P75：成功值放在 `Right`，解析、范围或奇偶校验失败时在 `Left` 中返回具体原因。

## 实现

### 方法一：do 记法

```haskell
import Text.Read (readMaybe)

eitherGoldbach :: String -> Either String (Integer, (Integer, Integer))
eitherGoldbach text = do
  n <- case readMaybe text of
    Nothing -> Left "not a number"
    Just x  -> Right x
  if n > 2 then Right () else Left "not greater than 2"
  if even n then Right () else Left "not an even number"
  pure (n, goldbach n)
```

`Either e` 的 Monad 实例遇到第一个 `Left e` 就停止后续计算，因此返回最先发现的错误。

### 方法二：显式 case 版

```haskell
eitherGoldbach :: String -> Either String (Integer, (Integer, Integer))
eitherGoldbach text =
  case readMaybe text of
    Nothing -> Left "not a number"
    Just n
      | n <= 2     -> Left "not greater than 2"
      | odd n      -> Left "not an even number"
      | otherwise -> Right (n, goldbach n)
```

不使用 Either Monad，纯 case 匹配。

## 方法对比

| 方法 | 错误策略 |
|------|----------|
| Either monad | 每项校验保留独立错误，首个 `Left` 自动短路 |
| 显式 case | 解析和 guard 集中在一处分支 |

## 测试

```haskell
>>> eitherGoldbach "28"
Right (28,(5,23))
>>> eitherGoldbach "1"
Left "not greater than 2"
>>> eitherGoldbach "27"
Left "not an even number"
>>> eitherGoldbach "hello"
Left "not a number"
```

## 要点总结

- `Maybe` 适合只关心成败的内部组合。
- `Either String` 更适合需要向调用者解释失败原因的边界接口。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P76.html)
- [Haskell 99 题学习路线](./)

---

[← P75 Maybe Monad](./p75) | [P77 List Monad →](./p77)
