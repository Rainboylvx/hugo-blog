---
title: "P90 - N 皇后问题"
date: 2026-07-12
draft: false
toc: true
tags: ["Haskell", "99题", "回溯"]
---

# P90 - N 皇后问题

> Find all solutions to the n queens problem
>
> 官方模块：`Problems.P90`　核心函数：`queens`

---

[← P89 二分图判定](./p89) | [P91 骑士巡游 →](./p91)

---

## 表示与剪枝

结果列表第 $c$ 个数表示第 $c$ 列皇后的行号。逐列放置时，候选行不能重复，并且与之前任一皇后不能满足 $|c-c'|=|r-r'|$。

## 实现

### 方法一：list monad 回溯

```haskell
queens :: Int -> [[Int]]
queens n
  | n < 0     = []
  | otherwise = place 1 [] [1..n]
  where
    place _ reversedRows [] = [reverse reversedRows]
    place column reversedRows availableRows = do
      row <- availableRows
      guard (safe column row reversedRows)
      place (column + 1) (row : reversedRows)
        (filter (/= row) availableRows)

    safe column row reversedRows = and
      [abs (column - previousColumn) /= abs (row - previousRow)
      | (previousColumn, previousRow) <- zip [column-1,column-2..1] reversedRows]
```

需要 `Control.Monad (guard)`。列表 Monad 自动收集每个合法分支的结果。

### 方法二：位掩码搜索

```haskell
import Data.Bits ((.&.), (.|.), bit, complement, shiftL, shiftR)

queens :: Int -> [[Int]]
queens n
  | n < 0     = []
  | otherwise = map columnsToRows (search 0 0 0 0 [])
  where
    full :: Integer
    full = bit n - 1

    search row columns diagonalLeft diagonalRight reversedColumns
      | row == n = [reverse reversedColumns]
      | otherwise = concat
          [ search (row + 1)
              (columns .|. columnBit)
              (((diagonalLeft .|. columnBit) `shiftL` 1) .&. full)
              ((diagonalRight .|. columnBit) `shiftR` 1)
              (column : reversedColumns)
          | column <- [0..n-1]
          , let columnBit = bit column
          , available .&. columnBit /= 0
          ]
      where
        available = full .&. complement
          (columns .|. diagonalLeft .|. diagonalRight)

    columnsToRows columnsByRow =
      [row + 1
      | column <- [0..n-1]
      , (row, placedColumn) <- zip [0..] columnsByRow
      , placedColumn == column]
```

这个版本逐行放置皇后，用整数的二进制位同时记录已占用的列和两组对角线。一个按位与即可得到整行候选位置，避免逐个比较此前皇后。最后把“每行所在列”转换回题目要求的“每列所在行”。

## 方法对比

| 方法 | 冲突状态 | 特点 |
|------|----------|------|
| list monad | 皇后行号列表 | 教学直观，适用于任意棋盘表示 |
| 位掩码 | 三个整数 | 常数更小，适合较大的 n |

## 测试

```haskell
>>> length (queens 8)
92
>>> minimum (queens 8)
[1,5,8,6,3,7,2,4]
>>> queens 1
[[1]]
```

最坏搜索仍是指数级，但行占用和对角线剪枝会在很浅的位置排除冲突。

## 参考

- [官方文档](https://ninetynine.haskell.chungyc.org/Problems-P90.html)
- [Haskell 99 题学习路线](./)

---

[← P89 二分图判定](./p89) | [P91 骑士巡游 →](./p91)
